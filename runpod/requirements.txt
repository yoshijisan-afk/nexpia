# infra/triposplat-selfhost/runpod/requirements.txt
#
# Part BBB（Gaussian Splatパイプライン拡張）BBB.13・ADR-320 PoC (J-6a): RunPod Serverless
# handler用の依存関係。
#
# 2026-07-12実機検証済み（RunPod L4 Pod上で実際に動作確認した組み合わせ）:
# torch(2.4.1+cu124で動作確認、ただし新規コンテナでは最新の安定版で問題ない)・torchvision・
# numpy・safetensors・pillow・tqdm・huggingface_hub。READMEに明記のとおり
# transformers・diffusers は不要（TripoSplat独自実装）。
#
# 2026-07-12再訂正: ベースイメージをServerless専用の`runpod/base`へ切り替えた
# （Dockerfile参照。`RUNPOD_WEBHOOK_GET_JOB`未注入によるクラッシュループの根本原因対応）。
# `runpod/base`はPyTorchを含まないため、torch/torchvisionを再度明記する。pip公式wheelは
# CUDA runtimeを同梱するため、ベースイメージのCUDAツールキット自体のバージョンと厳密に
# 一致させる必要はない想定（ただし実ビルドでの動作確認はまだ行っていない）。
# 2026-07-12追記: R2アップロード実装(handler.py参照)にboto3を明示追加。runpod SDKの推移的
# 依存として既に入っていることをビルドログで確認済みだが、直接使用するため明示する。
runpod>=1.7.0
torch
torchvision
numpy
safetensors
pillow
tqdm
huggingface_hub
requests
boto3
